# Lack Thereof

The source for <https://blog.lackthereof.info/>.

To resume development, read `PROJECT_STATUS.md`, `AGENTS.md`, and `docs/ENVIRONMENT.md`, then run `script/doctor`.

For new article images, follow `docs/IMAGE_UPLOADS.md`. Images belong in
Cloudinary rather than on `main`.
